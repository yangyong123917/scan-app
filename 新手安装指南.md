# 保姆级安装指南（完全新手版）

这份指南假设你从来没有用过 GitHub，也没有做过任何 App 开发。
只要照着顺序一步步做，大约 40 分钟能装好。

---

## 开始之前，先确认你有这些东西

- [ ] 一台 Windows 电脑
- [ ] 一部 iPhone，以及能连电脑的数据线
- [ ] 一个能收邮件的邮箱（注册 GitHub 用）
- [ ] 你的 Apple ID 账号密码（就是平时在 iPhone 上登录的那个）

另外，先确认你已经拿到了工程文件。它应该是一个叫 `scan-app` 的文件夹，
里面长这样：

```
scan-app
 ├── .github          （这个文件夹默认是隐藏的，暂时看不到没关系）
 ├── ScanLite
 │    ├── ScanLiteApp.swift
 │    ├── Core                 ← 2.0 新增
 │    │    ├── Models.swift
 │    │    ├── DocumentStore.swift
 │    │    ├── ImageTools.swift
 │    │    ├── DocumentScanner.swift
 │    │    ├── TextRecognizer.swift
 │    │    └── PDFBuilder.swift
 │    └── Views                ← 2.0 新增
 │         ├── LibraryView.swift
 │         ├── DocumentDetailView.swift
 │         ├── PageViewerView.swift
 │         ├── ScannerViews.swift
 │         ├── ExportSheet.swift
 │         └── SignaturePadView.swift
 ├── project.yml
 ├── README.md
 └── 新手安装指南.md
```

> 如果你之前装过 1.0 版，注意 2.0 把 `ContentView.swift`、`PDFExporter.swift`
> 和旧的 `DocumentScanner.swift`、`TextRecognizer.swift` 都删掉了，
> 换成了 `Core` / `Views` 两个子文件夹，替换文件夹时要整个换掉。

> 建议先把 `scan-app` 文件夹整个复制到桌面，方便等下找。

---

# 第 1 步：注册 GitHub 账号

如果你已经有 GitHub 账号，直接跳到第 2 步。

1. 打开浏览器，访问 `https://github.com`
2. 点右上角的 **Sign up** 按钮
3. 按提示填写：
   - **Email**：你的邮箱
   - **Password**：设置一个密码（至少 8 位，含数字和小写字母）
   - **Username**：起一个用户名，只能用英文和数字，比如 `zhangsan2026`
   - 国家/地区选 China
4. 会有一道拼图验证，按提示完成
5. 去邮箱收验证码，填进去
6. 注册完成，会进入 GitHub 首页

---

# 第 2 步：安装 GitHub Desktop

这一步是为了把工程文件传到 GitHub。用图形界面工具，不用敲命令。

1. 打开 `https://desktop.github.com`
2. 点 **Download for Windows**
3. 下载完成后，双击安装包，一路点 Next / Install
4. 安装完成后打开 **GitHub Desktop**
5. 点 **Sign in to GitHub.com**
6. 浏览器会自动打开，点 **Authorize desktop** 授权
7. 回到 GitHub Desktop，如果提示填名字和邮箱，随便填一个名字就行

---

# 第 3 步：把工程上传到 GitHub（重点）

## 3.1 把工程加入 GitHub Desktop

1. 在 GitHub Desktop 顶部菜单点 **File** → **Add local repository...**
2. 在弹出的窗口里点 **Choose...**，选中你桌面上那个 `scan-app` 文件夹
3. 点 **Add repository**

这时会出现一行红字提示：
> This directory does not appear to be a Git repository. Would you like to create a repository here instead?

4. 点那个蓝色的 **create a repository** 链接
5. 在新窗口里：
   - **Name** 保持 `scan-app` 就行，或者改成 `scanlite`
   - 其他都不用改
6. 点 **Create repository**
7. 如果左下角出现一个框让你填 Email，随便填你的邮箱，点 **Continue**

## 3.2 发布到 GitHub（这一步千万别选错）

1. 现在 GitHub Desktop 右上角应该有个按钮写着 **Publish repository**，点它
2. 弹出一个小窗口，里面有个勾选框：
   **Keep this code private**（保持代码私有）
3. **把这个勾去掉！必须去掉！**
   - 留着勾 = 私有仓库 = 构建要花钱 = 会失败
   - 去掉勾 = 公开仓库 = 构建免费
4. 点 **Publish repository**
5. 等几秒钟，右上角按钮会变成 "Fetch origin"，说明上传成功了

> ⚠️ **如果弹出 Error 窗口，里面写着 `fatal: unable to access ... port 443 ... Could not connect to server`**
>
> 这不是你操作错了，是**国内网络连 github.com 不稳定**。直接**再点一次 Publish repository**（或 Push origin）重试即可，通常试两三次就成功。
> 如果反复失败，见文末「常见问题排查 → 连不上 github.com」。

## 3.3 验证一下隐藏文件夹有没有传上去

这个很关键。`build.yml` 文件在 `.github` 文件夹里，而 Windows 默认不显示这种以点开头的文件夹。

1. 打开浏览器，访问 `https://github.com/你的用户名/scanlite`
   （用户名和仓库名换成你自己的）
2. 看一下文件列表里有没有 **.github** 这个文件夹
3. **如果没有**，按下面处理：
   - 回到 Windows 资源管理器，打开 `scan-app` 文件夹
   - 点顶部菜单 **查看** → 找到 **显示** → 勾选 **隐藏的项目**
   - 现在能看到 `.github` 文件夹了
   - 回到 GitHub Desktop，左下角 Summary 框里随便写一句 "add workflow"
   - 点 **Commit to main**
   - 再点右上角 **Push origin**
   - 刷新网页，`.github` 应该出现了

---

# 第 4 步：等待云端自动构建

1. 在浏览器里打开你的仓库页面
2. 点页面上方的 **Actions** 标签（在 Code 旁边）
3. 如果提示要不要启用 workflows，点绿色的按钮启用
4. 现在能看到一条任务记录，前面有个**黄色小圆点**（表示正在运行）
5. 什么都不用做，等 3 到 6 分钟
6. 刷新页面，黄色圆点变成**绿色对勾**，就成功了

> 这个工程很小，实测云端构建只要 **1 分钟左右**（17:39:53 开始 → 17:40:53 结束），看到这么快成功是正常的，不是出错。
> 如果变成**红色叉**，说明构建失败。点进去看日志，把报错内容发给助手。

---

# 第 5 步：下载 IPA 文件

1. 点进那条绿色对勾的任务
2. 页面**一直往下滚**，滚到最底部
3. 会看到 **Artifacts** 区域，里面有一项：`ScanLite-unsigned-ipa`
4. 点它下载，得到一个 zip 压缩包
5. 解压这个 zip，里面是 `ScanLite-unsigned.ipa`
6. 记住这个文件放在哪，等一下要用

---

# 第 6 步：安装 iPhone 连接驱动

这一步的目的只有一个：**让 Windows 认识你的 iPhone**。
不装的话，第 8 步 Sideloadly 会发现不了手机，界面上的设备框是空的。

原理先说明白：Windows 默认只把 iPhone 当"充电设备"，
需要苹果的 **Apple Mobile Device 驱动**（随苹果软件一起装）。
有两个来源，**选一个装就行，千万不要两个都装**。

---

## 先看你的 Windows 版本（决定走哪条路线）

1. 按键盘 `Win` 键（就是左下角那个窗户图标键）
2. 直接打字：`关于你的电脑`（或 `about`）
3. 回车打开，看两行：
   - **版本**：比如 22H2 / 23H2
   - **系统类型**：64 位操作系统 / 32 位操作系统

---

## 路线 A：用微软商店装（推荐，最省事）

**前提**：Win11 任意版本，或 Win10 版本号 ≥ 22H2（内部版本 19045）。
低于这个版本，微软商店里搜不到，直接看路线 B。

1. 点开 Windows 开始菜单（左下角窗户图标）
2. 找到并打开 **Microsoft Store**（图标是个购物袋）
   - 开始菜单里找不到？直接按 `Win`，打字 `商店`，回车
   - 第一次打开可能需要登录微软账号，有账号登录一下，没有就点"以后再说"
3. 在商店顶部的搜索框里输入：`Apple Devices`
4. 结果里找**发布者是 Apple Inc.** 的那一个（图标是白色背景 + 灰色设备图案）
   - 点进去后，页面下方写着 "Publisher: Apple Inc." 才是正版
5. 点 **获取**（免费应用会显示"获取"，有时显示"安装"）
6. 等进度条走完，按钮变成 **打开** 或 **已安装**，就装好了
7. 顺手再搜一次 `iCloud`，同样安装（同一个发布者 Apple Inc.）
8. **重启一次电脑**（这一步别省，驱动要重启才生效）

> **搜不到 Apple Devices 怎么办？**
> 说明你的商店区域或系统版本不支持。按下面任一处理：
> - 商店右上角头像 → 设置 → 把所在地区临时改成"美国"，重新搜索安装，
>   装完改回来即可
> - 或者直接走路线 B，效果一样

---

## 路线 B：用苹果官网的桌面版 iTunes（兼容性最好）

Sideloadly 官方文档推荐的就是这个方式。如果你的商店搜不到 Apple Devices，
或者第 8 步 Sideloadly 死活认不出手机，就用这条。

**装之前先检查**：如果你电脑里已经有"微软商店版 iTunes"或"Apple Devices"，
先去 **设置 → 应用 → 已安装的应用** 里把它**卸载**，并重启电脑。
两套混装一定会出问题。

1. 浏览器打开：`https://www.apple.com/itunes/download/win64`
   （这是苹果官网直链，64 位系统用这个）
2. 页面会自动开始下载一个叫 `iTunes64Setup.exe` 的文件
3. 下载完双击它，如果弹出"是否允许此应用更改你的设备"，点 **是**
4. 一路 **下一步 / Next**，勾选同意条款，点 **安装**
   - 安装过程中会自动装上一堆组件，名字里有
     **Apple Mobile Device Support**、Apple Application Support、Bonjour，
     **这些都是驱动，全部保留，不要取消勾选**
5. 装完点 **完成**
6. 再装一个 iCloud：浏览器打开 `https://support.apple.com/zh-cn/106510`
   ，按页面提示下载安装 Windows 版 iCloud
7. 装完**重启电脑**

> 装完 iTunes 后可能会自动打开 iTunes 主界面，直接关掉就行，
> 我们不用它的音乐功能，只要它带的驱动。

---

## 验证驱动装好了没有（两条路线都要做）

1. 用数据线把 iPhone 连到电脑（**原装线或质量好的线**，劣质线经常连不上）
2. iPhone 屏幕会弹出 **"要信任此电脑吗？"** → 点 **信任** → 输入手机锁屏密码
   - 没弹？先把手机解锁，再重新插一次数据线
3. 电脑上打开"此电脑"（桌面上的图标，或按 `Win + E`）
4. 左侧列表里应该能看到你的 iPhone 名字
5. 更进一步：开始菜单打开 **Apple Devices**（路线 A）或 **iTunes**（路线 B），
   里面能看到设备名称、容量、电量 → 说明驱动完全正常

驱动装好的标志就三条：**手机弹了信任框、此电脑里能看到手机、
苹果软件里能看到设备信息**。三条都满足，再去第 7 步。

---

## 这一步常见卡壳

| 现象 | 原因和处理 |
|---|---|
| 商店里搜不到 Apple Devices | Win10 版本太低，或商店区域限制 → 走路线 B |
| 商店点安装后报错 0x8… | 商店缓存坏了：按 `Win + R` 输入 `wsreset.exe` 回车，等黑窗口自动关闭后重试 |
| 连上手机但"此电脑"里没有 | 换一根数据线、换一个 USB 口（优先用主机后面的口）；还不行就装路线 B |
| 手机一直没弹"信任"框 | 手机先解锁；或设置 → 面容 ID 与密码 → 拉到最下面 → 打开"USB 配件" |
| 装完 iTunes 反而更乱 | 你可能同时装了商店版和官网版 → 全部卸载，重启后只装一套 |

---

# 第 7 步：安装 Sideloadly

1. 打开浏览器，访问 `https://sideloadly.io`
2. 点 **Download for Windows**
3. 下载完双击安装
4. 如果 Windows 弹出"已保护你的电脑"的蓝色提示，点 **更多信息** → **仍要运行**
5. 安装完成后打开 Sideloadly

---

# 第 8 步：签名并安装到 iPhone（核心步骤）

1. 用数据线把 iPhone 连到电脑，手机上点**信任**并输入密码
2. 打开 Sideloadly

## 8.1 先认清 v0.60 的界面（和老教程不一样）

```
┌──────────────────────────────────────────────┐
│ [IPA大图标]  iDevice:  [设备下拉框]  [图标][图标] │
│  └ <none>     Apple ID:┌──────────────────┐  │
│                        └──────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │           Advanced Options             │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │                 Start                  │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

| 界面上这块 | 作用 |
|---|---|
| 左上角大的 **IPA** 图标，下面写着 `<none>` | 放 IPA 文件的地方。`<none>` 表示还没放文件 |
| 右边的 **iDevice** 下拉框 | 显示检测到的手机。**能看到你的 iPhone 名字 = 驱动正常** |
| 下拉框右边的数字按钮 | 刷新设备列表，手机没显示时点它 |
| **Apple ID** 输入框 | 填你的 Apple ID 邮箱，带下拉箭头可选用过的账号 |
| **Advanced Options** 按钮 | 高级选项。v0.60 里"改 Bundle ID"这个开关叫 **Use automatic bundle ID** |
| **Start** 按钮 | 开始签名安装 |
| 右上角眼睛带斜杠的图标 | "隐藏敏感信息"，截图发别人前点一下，会遮住邮箱和设备号 |

> **注意：v0.60 已经没有单独的 Password 输入框了。**
> 密码不是在这里填，而是点 Start 之后弹窗才让你输。这是正常的。

## 8.1.1 Advanced Options 面板说明（点开后）

| 选项 | 要不要动 |
|---|---|
| Try to support older iOS versions (7+) | 不动 |
| Enable File Sharing | 不动（勾了可以用文件 App 看 App 里的文档） |
| **Change app name** | 想改桌面上的 App 名字才勾。勾上后在右边框里填中文名 |
| **Use automatic bundle ID** | **保持勾选！** 就是老教程说的 Change Bundle ID |
| Remove limitation on supported devices | 不动 |
| Change app version | 不动 |
| Use custom entitlements | 不动 |
| Tweak Injection / Inject dylibs/frameworks | 不动（这是给插件用的，我们用不到） |

> 只有在报 "bundle identifier is not available" 时才需要动它：
> 把 **Use automatic bundle ID 取消勾选**，然后在输入框里手工填一个
> `com.你的名字.scanlite`（名字用英文，越独特越好）。

## 8.2 操作顺序

1. 把 `ScanLite-unsigned.ipa` 文件**直接拖到左上角那个 IPA 大图标上**
   - 拖对了的话，图标下方的 `<none>` 会变成文件名
   - 没反应就多拖几次，或者把文件放在桌面再拖
2. 在 **Apple ID** 里填你的 Apple ID 邮箱（比如 `xxx@qq.com`），密码框不用管
3. 点 **Advanced Options**，确认里面 **`Use automatic bundle ID` 是勾选状态**
   - 老教程里叫 **Change Bundle ID**，v0.60 改了名字，功能一样
   - **这个选项默认就是勾上的**，通常什么都不用动
   - 它的作用是让 Sideloadly 自动给 App 换一个随机标识符，
     避免和别人已占用的 ID 冲突（报错内容通常是 "bundle identifier is not available"）
4. 点 **Start**
5. **这时才会弹出窗口要 Apple ID 密码**，输入后确定
   - 如果你的 Apple ID 开了两步验证，要用 **App 专用密码**，
     不能用登录密码。生成方法见下面"常见问题排查"
6. 等待进度条走完，底部出现 **Done**，就装好了
7. 装完 Sideloadly 可能会提示要不要自动刷新，可以先跳过

## 8.3 iPhone 会提示"需要启用开发者模式"（iOS 16 及以上必做）

装的过程中，iPhone 上会弹出提示，要求你启用**开发者模式**。
这是 iOS 16 之后苹果加的安全机制，**必须开，绕不过去**。
（提示原文可能是："需要在设置中启用开发者模式，才能使用此 App"）

### 开启步骤

1. iPhone 上打开 **设置**
2. 往下滑，找到并点 **隐私与安全性**（不是"通用"！）
3. **一直滑到页面最底部**
4. 会看到 **开发者模式** 这一项，点进去
5. 把开关**打开**（变绿）
6. 手机会弹窗提示"需要重新启动才能生效"，点 **重新启动**
7. 手机重启完成后，屏幕上会**再弹一个窗口**问你要不要打开开发者模式
   → 点 **打开**，然后输入手机锁屏密码
8. 看到开关变成绿色、状态显示已开启，就完成了

### 开完之后

回到电脑上，**再点一次 Sideloadly 的 Start**，重新装一遍 App。
这次就不会再被拦住了，等进度条走完出现 **Done** 即可。

### 说明

- **找不到"开发者模式"这一项？** 说明还没走到需要它的那一步。
  一定要先在 Sideloadly 里点过一次 Start（触发安装失败），
  这一项才会出现在设置里。找不到就回上一步再点一次 Start。
- **开关点不动 / 是灰色的？** 把 iPhone 解锁、然后重新插一次数据线再试。
- **开启后会不会有风险？** 不会。它只是允许安装不是从 App Store 下载的 App，
  不影响手机正常使用，也不需要越狱。以后不想用了随时可以关掉。
- 开发者模式**开一次就长期有效**，以后重签 App 不用再开。

---

# 第 9 步：在 iPhone 上信任这个 App

刚装好的 App 还不能打开，会提示"不受信任的开发者"。

1. 打开 iPhone 的 **设置**
2. 点 **通用**
3. 往下滚，点 **VPN 与设备管理**
4. 在"开发者 App"下面会看到你的 Apple ID 邮箱，点进去
5. 点 **信任 "你的Apple ID"**，再点一次"信任"确认
6. 回到桌面，App 图标变成正常颜色，点开就能用了

> 找不到"VPN 与设备管理"？说明第 8 步没装成功，回去检查。
> 这个入口只有在用数据线连过电脑并装过 App 之后才会出现。
> 也可能是**开发者模式没开**（见 8.3），先把它开了再重新装一次。

---

# 第 10 步：处理"7 天过期"问题

免费 Apple ID 签名的 App，**7 天后会打不开**（图标变灰）。
这是苹果的限制，绕不过去。你有两个选择：

## 选择一：手动重签（简单，但需要每 7 天做一次）

到期后，重复第 8 步和第 9 步就行，大概 3 分钟。

## 选择二：AltStore 自动续签（推荐，一劳永逸）

装了 AltStore 之后，只要 iPhone 和电脑连着同一个 WiFi，
它会在后台自动帮你续签，你完全不用管。

1. 打开 `https://altstore.io`
2. 下载 **AltServer for Windows**
3. 安装后运行 AltServer（它会在右下角任务栏出现一个小图标）
4. 用数据线连接 iPhone
5. 点任务栏的 AltServer 图标 → **Install AltStore** → 选择你的 iPhone
6. 会要求输入 Apple ID 和密码，输入后等待安装完成
7. iPhone 上会出现 **AltStore** 图标
8. 去 **设置 → 通用 → VPN 与设备管理** 里信任 AltStore
9. 打开 AltStore，用**同一个 Apple ID** 登录
10. 打开 **Settings**，把 **Background Refresh** 打开
11. 以后只要 iPhone 和电脑在同一个 WiFi 下，就会自动续签

---

# 第 11 步：升级到新版本

升级流程和第一次装差不多，只有两个地方要注意。

各版本改了什么：

| 版本 | 新增内容 |
|---|---|
| **2.0** | 相机扫描（手机变扫描仪）、文件管理、PDF 编辑（水印/页码/签名/密码/可搜索） |
| **2.1** | 补上 **App 图标**。2.0 装完桌面图标是空白方块，就是缺这个 |
| **2.2** | **文字汇总**：多份文件的文字按顺序拼成一份稿子，导出 Word / PDF / 纯文本 / Markdown |
| **2.2.1** | 修复 2.2 的**构建失败**（代码里用了一个只有导入 SwiftUI 才能调的方法，见 11.6），并让报错自动跳到页面顶部 |

## 11.1 先把新代码提交并推送

> **注意**：上次推的 2.2 版本在云端构建时失败了（红色叉），只改了代码没生成出 IPA。
> 这次的 2.2.1 已经修好，请按下面重新提交推送一遍。**上次那笔提交不用撤销，直接再提一次即可。**

1. 新文件已经**直接放进你加进 GitHub Desktop 的那个 `scan-app` 文件夹**了，不用手动搬
   - 2.2 本次新增：`ScanLite/Core/TextCompilation.swift`、`ScanLite/Core/DocxWriter.swift`、
     `ScanLite/Core/TextPDFWriter.swift`、`ScanLite/Views/CompilationViews.swift`
   - 2.2 本次修改：`ScanLiteApp.swift`（底部多了两个页签）、`DocumentStore.swift`、
     `LibraryView.swift`、`DocumentDetailView.swift`、`README.md`、`project.yml`（版本号升到 2.2）
   - 2.2.1 本次修改：`DocumentStore.swift`（修 bug）、`TextPDFWriter.swift`（写法收紧）、
     `.github/workflows/build.yml`（新增"报错自动跳到页面顶部"）、`project.yml`（版本号 2.2.1）
2. 打开 **GitHub Desktop**，左边 **Changes** 会自动列出这些变化
3. 左下角 Summary 框里写一句 `fix build error`（或 `add text compilation`）
4. 点 **Commit to main**
5. 点右上角 **Push origin**
   （如果弹出 `port 443` 的报错，直接再点一次，见文末排查章节）
6. 浏览器打开仓库 → **Actions** → 等新构建跑完（约 1 分钟）
7. 底部 **Artifacts** 下载新的 `ScanLite-unsigned-ipa`

> 如果 Actions 里出现的是**红色叉**，点进去把报错发给我，我来改代码。

## 11.2 安装时注意 bundle ID（关键）

2.0 版开始，建议**固定一个 bundle ID**，这样以后升级是覆盖安装，App 里存的东西不会丢。

1. 先在 iPhone 上**长按旧的「扫描小工具」图标 → 删除 App**
   （旧版里没有你重要的扫描件才删；有的话先导出 PDF 再删）
2. 打开 Sideloadly，把新 IPA 拖进去，填 Apple ID
3. 展开 **Advanced Options**
4. **把 `Use automatic bundle ID` 前面的勾去掉**
5. 在它下面的输入框里填一个固定的 ID，例如：`com.yangxh.scanlite`
   （把 yangxh 换成你的英文名或拼音，中文不行）
6. 点 **Start** → 输密码 → 等 **Done**

以后每次升级都填**同一个 ID**，就是覆盖安装，数据保留。

## 11.3 装完检查一下

打开 App，底部会看到**两个页签**：**扫描件** 和 **文字汇总**。

**「扫描件」页**（原首页）：

- 点右上角 **+** 有三个入口：相机扫描 / 从相册导入 / 导入 PDF
- 点 **相机扫描**，会直接进入苹果原生的扫描界面——这就是"手机变扫描仪"
- 扫完一份多页文档，点进去可以拖动排序、旋转、切滤镜
- 点页面上的 **导出**，能看到页码、水印、签名、密码、可搜索 PDF 这些选项

**「文字汇总」页**（2.2 新增）：

- 点右上角 **+** 新建一份汇总，随便起个名字
- 进去后点左下角 **追加文字**，有两个常用入口：
  - **扫描一份并追加** —— 直接开相机拍，拍完自动识别文字、自动接到末尾。**这就是"扫一份接一份"的用法**
  - **从已有扫描件选择** —— 从扫过的文档里挑，可以多选，**点击的先后就是排列顺序**
- 回到汇总页，点某一条可以改错字；点右上角「编辑」可以拖动调整顺序、左滑删除
- 点右下角 **导出成一份文件**，选格式 → 生成 → 分享到「文件」App 或发给别人

## 11.4 关于 App 图标（2.1 新增）

**为什么之前桌面上的图标是空白方块？**
2.0 的工程里**根本没有图标资源**，`project.yml` 里也没告诉 Xcode 用哪个图标，
所以 iOS 只能显示系统默认的灰白占位方块。不是装坏了，也不是签名问题。

2.1 补上了两样东西：

- `ScanLite/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png` —— 1024×1024 的图标图片
- `project.yml` 里加了 `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` —— 告诉 Xcode 用这个名字的图标

> **装完图标还是空白？** iOS 会缓存图标。先去手机上**长按图标 → 删除 App**，再重新装一次，新图标就会出现。

### 以后想换图标，两种办法

**办法一：只换图片（最简单）**

把自己准备的 1024×1024 PNG 改名成 `AppIcon-1024.png`，覆盖
`ScanLite/Assets.xcassets/AppIcon.appiconset/` 里的同名文件，然后重新 Commit → Push → 重装。

⚠️ 三个硬性要求，不满足会构建失败或显示异常：

- 必须是 **1024×1024 正方形**
- 必须是 **PNG**，且**不能带透明通道**（iOS 不接受带 alpha 的 App 图标）
- **不要自己画圆角**。iOS 会自动裁圆角，自己加会变成"圆角套圆角"的怪形状

**办法二：改颜色重新生成**

现在这个图标是用脚本画出来的，不是网上找的。脚本在 `tools/make_app_icon.py`，
开头几行是配色（`BG_TOP` 顶部蓝、`BG_BOT` 底部蓝、`BRACKET` 扫描框、`LINE` 文字线）。
改完颜色在本机跑一遍就行：

```
python tools/make_app_icon.py ScanLite/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png
```

改完同样 Commit → Push → 重装。

## 11.5 文字汇总怎么导出（2.2 新增）

导出时有四种格式可选，按用途挑：

| 格式 | 什么时候用 |
|---|---|
| **Word 文档 `.docx`** | 需要在电脑上继续排版、改格式、加页眉页脚 |
| **PDF（文字版）** | 要打印、要提交给别人看，且希望对方能搜索关键词 |
| **纯文本 `.txt`** | 只是要文字本身，粘到哪儿都行 |
| **Markdown `.md`** | 要放进笔记软件（Obsidian、语雀、Notion 等） |

下面还有几个开关：

- **开头写入名称和字数统计** —— 生成稿子开头会有一段"生成时间／共几份／多少字"
- **每份前面写上名称** —— 每份文字前面加一行 `1. 文档名`，方便分辨来源
- **每份之间加分隔线** —— 纯文本和 Markdown 才有
- **显示页码** —— 只有 PDF 才有

> **PDF 和扫描件导出的 PDF 不一样**：这里导出的是**真正的文字**（矢量），不是图片。
> 所以文件极小（几十页通常不到 100 KB）、能选中、能复制、能全文搜索。
> 扫描件导出的 PDF 是图片，体积大得多。

生成完点 **分享 / 保存文件**，可以存到「文件」App、发微信、发邮件。

## 11.6 2.2 那次构建为什么失败（2.2.1 已修复）

失败的是云端编译这一步，报的是 `exit code 65`（xcodebuild 编译失败），原因是一行代码：

```swift
// ❌ 会编译不过
compilations[index].items.move(fromOffsets: offsets, toOffset: destination)
```

`move(fromOffsets:toOffset:)` 这个方法**看起来很标准库，实际上定义在 SwiftUI 框架里**。
写上它的那个文件（`DocumentStore.swift`）只导入了 `Foundation` 和 `UIKit`，
Swift 的规矩是：扩展方法必须在**使用它的那个文件里**导入对应框架才能调用 ——
所以编译器直接报 "has no member 'move'"。

后来改成手写一遍同样的逻辑（先把要移动的元素摘出来，再算插入点），
不依赖任何界面框架，问题就没有了。同一个文件里的拖拽排序功能完全不受影响。

> 顺带一提：这次还改了 `build.yml`，以后构建失败时，
> 错误行会自动出现在任务页面**顶部**的注释区，截图发我就能定位，不用再翻几千行日志。

---

# 常见问题排查

## 问题：连不上 github.com（推送失败 / 网页打不开）

**现象**：GitHub Desktop 弹出 Error 窗口，内容是
`fatal: unable to access 'https://github.com/你的用户名/仓库.git/': Failed to connect to github.com port 443 after xxxxx ms: Could not connect to server`
或者浏览器打开 github.com 一直转圈 / 超时。

**原因**：不是你的设置或代码有问题，而是**国内网络到 github.com 这条线路不稳定**，时通时不通。
（实测同一台电脑连测 6 次，成功 2 次、超时 4 次；而 GitHub 的另一个 IP 连 4 次全部成功。）

**处理顺序**：

1. **先无脑重试。** GitHub Desktop 再点一次 **Push origin** 或 **Publish repository**。
   网页就按 `F5` 刷新几次。大概三分之一到一半的概率一次就过。
2. **重试几次还不行 → 用固定 IP 脚本（推荐，一劳永逸）**
   桌面上的 **`fix-github-host.bat`**：
   - **右键**这个文件 → **以管理员身份运行**（必须右键，直接双击会因为权限不够失败）
   - 看到 `DONE` 后关掉窗口
   - **把浏览器全部关掉再重新打开**（浏览器会缓存 DNS，不关没用）
   - 再重试推送 / 刷新网页
   - 这个脚本会把 `github.com` 固定到实测更稳的 IP（`140.82.113.3`），并自动备份原来的 hosts 文件。想撤销就把备份 `hosts.scanlite.bak` 覆盖回 hosts
3. **还不行 → 用你电脑上已装的代理工具**
   你这台机器装过 **Clash Verge Rev**（在 `%APPDATA%\io.github.clash-verge-rev.clash-verge-rev`）。
   打开它 → 打开「系统代理」或「TUN 模式」→ 浏览器和 GitHub Desktop 都会走代理，问题直接消失。
   （如果你有「雷神加速器」「外星仔加速器」，那是游戏加速器，对 GitHub 没用，不用开。）

**判断有没有恢复**：浏览器登录 `https://github.com` 能秒开、仓库页面能刷出来，就说明通了。

## 问题：GitHub Actions 显示红色叉，构建失败

从 2.2.1 起，构建失败时**错误会自动跳到任务页面最上方**，不用再翻日志：

1. 点进那次失败的运行（红色叉）
2. 页面**顶部**会出现一个 **Annotations / 注释** 区块，里面用红字列着
   形如 `xxx.swift:123:45: error: ...` 的行——**把这个区块截图发我**就够了
3. 如果顶部没看到，就点左边那个红色的 **build** 步骤，往下找红字

另外两种常见情况：
- 提示找不到 scheme：把 `build.yml` 里的 `macos-latest` 改成 `macos-15`
- 提示找不到 xcodegen：可能是网络抖动，点右上角 **Re-run jobs** 重试一次

## 问题：Sideloadly 界面上找不到 Password 输入框

这是正常的。Sideloadly v0.60 把密码框去掉了，密码改在**点 Start 之后弹窗**里输入。
网上老教程里的"Password 留空"已经过时。

## 问题：Sideloadly 里设备框显示不出来手机

- 先确认手机已解锁、数据线已插好，然后点设备下拉框右边的**刷新按钮**
- 还是不行：关掉 Sideloadly 重开；再不行就重新插拔数据线
- 一直不行说明驱动没装好，回到第 6 步

## 问题：IPA 拖进去没反应 / `<none>` 不变

- 把 IPA 文件先放到桌面，再拖到左上角那个大 IPA 图标**正中间**
- 或者先把 Sideloadly 用管理员身份打开（右键图标 → 以管理员身份运行）再拖

## 问题：手机上一直提示"需要启用开发者模式"

1. 打开 iPhone 的 **设置 → 隐私与安全性**，**滑到最底部**
2. 点 **开发者模式** → 把开关打开 → 按提示**重启手机**
3. 重启后屏幕上会再弹窗确认，点 **打开** → 输锁屏密码
4. 回到电脑，**重新点一次 Sideloadly 的 Start**

找不到"开发者模式"这一项？先在 Sideloadly 里点过一次 Start，它才会出现。

## 问题：Sideloadly 报 "bundle identifier is not available"

说明选择的 bundle ID 已经被别人占了（因为工程里的默认 ID 是 `com.example.scanlite`，
很可能被占用）。

但 **v0.60 默认已经勾选了 `Use automatic bundle ID`**，会自动换一个随机 ID，
正常情况下不会报这个错。如果还是报了：

1. 回到 Advanced Options，确认 **Use automatic bundle ID** 是勾选的
2. 还是不行就取消勾选，在输入框里手工填
   `com.你的名字.scanlite`（名字用英文，越独特越好）

## 问题：Sideloadly 报 "wrong password" 或一直转圈

两点检查：
1. Apple ID 密码里有没有特殊字符，重新输一遍
2. 如果开了两步验证，必须用 **App 专用密码**，不是登录密码。
   生成方法：浏览器打开 `appleid.apple.com` 登录 →
   登录与安全 → App 专用密码 → 生成

## 问题：手机上找不到"信任开发者"的入口

说明 App 根本没装上去。重新做第 8 步，注意看 Sideloadly 最后有没有显示 Done。

## 问题：App 装好后闪退

大概率是 iOS 版本太低。这个工程要求 **iOS 16 或更高**。
查看方法：设置 → 通用 → 关于本机 → 软件版本。

## 问题：想改 App 名字

1. 打开 `project.yml`，找到 `CFBundleDisplayName: 扫描小工具`
2. 改成你喜欢的名字
3. 在 GitHub Desktop 里 Commit + Push
4. 等 Actions 重新跑完，重新下载 IPA 安装

## 问题：想重新装一次

重复第 5 步到第 9 步即可。

## 问题：文字汇总里某一份是空的 / 写着"没识别到文字"

三种可能：

1. **照片糊了或反光** —— 回「扫描件」页打开那份文档，看看图片清不清楚
2. **页面是手写体或艺术字** —— 系统 OCR 对手写识别不稳，正常现象。可以点进那一条**手动补字**
3. **换过页面顺序或旋转过** —— 长按那一条 → **重新识别并追加一份**；或者先删掉旧的

## 问题：导出的 Word 打不开，提示"文件已损坏"

`.docx` 是手工拼的 ZIP 包，最怕正文里有特殊控制字符（代码里已经做了过滤和转义）。
先试试换成 **PDF** 或 **纯文本** 导出，能正常的话把汇总内容发我，我再针对性修。

## 问题：文字汇总导出的顺序和我想要的不一样

导出顺序 = 汇总页里条目的排列顺序。

- 从「扫描件」页多选追加时，**顺序就是你点选的先后**
- 想调整：进汇总页 → 点右上角 **编辑** → 拖动调整 → 点 **完成**

## 问题：不小心删掉了汇总里的一条

没有撤销功能，但可以补回来：
从「扫描件」页找到那份原始文档 → 长按 → **加入文字汇总** → 选同一份汇总，它会加到末尾，
再用「编辑」拖到想要的位置。

> 提醒：**删除 App 会连带删掉里面所有扫描件和汇总**。重要的先导出成文件。

---

# 简洁版流程回顾

```
1. 注册 GitHub 账号
2. 装 GitHub Desktop，把 scan-app 加进去
3. Publish repository（记得取消勾选 private）
4. 等 Actions 出现绿色对勾
5. 下载 Artifact 里的 IPA
6. 装 Apple Devices + iCloud 驱动
7. 装 Sideloadly
8. 连 iPhone，拖入 IPA，填 Apple ID，确认 Use automatic bundle ID 已勾选，Start
9. iPhone 提示时去 设置 → 隐私与安全性 → 开发者模式 → 打开 → 重启手机 → 再点一次 Start
10. iPhone 设置里信任开发者（设置 → 通用 → VPN 与设备管理）
11. 装 AltStore 解决 7 天过期

## 以后升级新版本

1. 用新的 scan-app 文件夹替换旧的
2. GitHub Desktop 里 Commit → Push
3. 等 Actions 跑完，下载新 IPA
4. Sideloadly 里取消勾选 Use automatic bundle ID，填固定的 com.你的名字.scanlite
5. Start，装完就是覆盖升级，App 里的文档和汇总都不会丢
```
